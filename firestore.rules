rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users in this class project to read/write app data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}